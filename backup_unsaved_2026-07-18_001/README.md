Backup de cambios no comiteados

Qué incluye:
- Copias de `index.html`, `api/chat.js`, `.env.example`, y configuración de VSCode.

Restaurar:
- Copia manualmente los archivos desde `backup_unsaved_2026-07-18_001/` a la raíz del proyecto.

Sugerencias:
- Cuando tengas `git` disponible, puedes crear una rama y aplicar los archivos manualmente o usar `git stash`.
- No olvides no subir tus claves a GitHub; coloca `GROQ_API_KEY` en variables de entorno.
